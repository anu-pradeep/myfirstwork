void main()
{
  int ? age =20 ; // by adding ? here means, the value may be null
  var result = age ?? "no value";
  print(result);
}
// ? null aware operator
// ?? null operator
// ! null check
// ? : condition operator

